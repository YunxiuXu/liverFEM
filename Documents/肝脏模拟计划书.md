# 肝脏模拟论文：一步一步通关指南

### 第一步：环境与代码准备（已完成）
- [x] **各向异性支持**：代码已修改。
- [x] **自动化测试**：按 `X`/`Y` 键触发。
- [x] **数据记录**：生成 `force_data_x/y.txt`。

---

### 第二步：实验 3 - 各向异性验证（已完成）
- [x] 设置参数：`youngs1=10e6`, `youngs2=1e6`。
- [x] 运行测试：生成了数据，差异显著（23%）。
- [x] **待办**：保存生成的图表 `anisotropy_result.png` 用于论文 Fig. 5。

---

### 第三步：实验 2 - 不可压缩稳定性（现在做这个！）
*目标：证明泊松比设为 0.49 时，模拟依然稳定且体积保持良好。*

1.  **修改 `TetgenFEM/parameters.txt`**：
    *   `poisson=0.49` (甚至 0.499)
    *   `youngs=1000000`
    *   `youngs1=1000000` (暂时设回各向同性，排除干扰)
    *   `youngs2=1000000`
    *   `youngs3=1000000`
2.  **运行程序**：
    *   `./TetgenFEM`
    *   用鼠标右键剧烈拖拽肝脏。
3.  **观察与记录**：
    *   模型是否爆炸？（如果不炸，Passed）
    *   拉伸时体积是否保持？（如果没变成纸片，Passed）
    *   **截图**：截一张大变形的图，保存为 `incompressible_test.png`。

---

### 第四步：实验 1 - 性能基准测试
*目标：记录 FPS 数据。*

1.  **修改 `TetgenFEM/parameters.txt`**：
    *   恢复 `poisson=0.45`。
    *   调整 `groupNumX/Y/Z` (如 8x8x8)，看控制台输出的 Total Tetrahedra 数量。
2.  **运行记录**：
    *   记录：四面体数量 (Tetras)
    *   记录：平均 FPS
    *   (可选) 如果有 VBD 代码，跑一下同样的模型，记录它的 FPS 对比。

---

### 第五步：论文填空
将上述数据填入论文模板。
